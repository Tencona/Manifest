import Item from '@/Manifest/models/item';
import Property from '@/Manifest/models/property';
import Type from '@/Manifest/models/type';
import { Result } from '@/Manifest/models/result'; // Move this elsewhere? It's not part of the data model

export default {
	Item,
	Property,
	Result,
	Type,
};
